#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
	int result = a * b;
		return (result);
}
